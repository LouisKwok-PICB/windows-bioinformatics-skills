interface:
  display_name: "Paper Figure Assembler"
  short_description: "Evidence-hierarchy multi-panel figure assembly."
  default_prompt: "Use $paper-figure-assembler to assemble publication-ready multi-panel scientific figures from live R objects after panel evidence gates pass, assigning area by evidence hierarchy, information density, natural aspect ratio, and final journal readability."
