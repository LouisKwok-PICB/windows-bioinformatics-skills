interface:
  display_name: "Publication Plot Styler"
  short_description: "Evidence-aware single-panel plot styling."
  default_prompt: "Use $publication-plot-styler to convert a supported evidence-chain result into a reader-facing publication plot, checking terminology, display grammar, legends, color scales, whitespace, final-size readability, and PDF/PNG/TIFF export settings."
